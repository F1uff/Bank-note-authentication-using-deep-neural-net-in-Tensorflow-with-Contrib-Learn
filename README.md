#Bank note authentication using deep neural net, in Tensorflow, with Contrib Learn

Quick(*) play around with Tensorflow using Contrib.Learn

Using the UCI repository's banknote authentication data set.
